{
  "name": "@dimelords/weather-plugin",
  "private": false,
  "version": "2.9.0",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c -w",
    "lint": "eslint src/**/*.ts*",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  }
}
